// colocar aqui o arquivo que será testado

test("Deve calcular os dividendos corretamente", () => {
  expect(calcular(1000, 12)).toBe(1000.12);
  expect(calcular(100, 2)).not.toBe(200);
});

test("Teste de cálculo com valor negativo", () => {
Precisa dar erro, o valor não pode ser negativo
});

test("Teste de calculo com o mês negativo", () => {
Precisa dar erro, o valor não pode ser negativo
});

teste de taxa de juros diferente
